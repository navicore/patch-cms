[package]
name = "cms-core"
description = "CMS file system model — fn ft fm addressing and minidisk operations"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
xedit-core = { path = "../xedit-core", default-features = false }

[dev-dependencies]
tempfile = "3"
